"""Browser automation modules (Playwright).

Provides passive page capture (``capture``) and active AI-driven
interaction (``agent``, ``actions``, ``dom_extractor``, ``llm_client``).

Anti-detection is handled by ``stealth`` (proxy rotation, fingerprint
randomization) and ``captcha`` (detection and graceful degradation).
"""
